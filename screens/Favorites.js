import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export default () => {
  return (
    <View>
      <Text>FAVORITES</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
